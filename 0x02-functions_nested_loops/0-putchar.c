#include <stdio.h>
#include "main.h"

/**
 *main - entry point
 *
 *Description: a function of c to
 *
 *Return: always 0 (success)
*/
int main(void)
{
	char list[] =  "_putchar";
	int count;
i
	for (count = 0; count < 8; count++)
		_putchar(list[count]);

	_putchar('\n');
	return (0);
}
