#include "main.h"

/**
 *_isalpha - entry point
 *
 *@c: a function of c to check lower
 *
 *Return: 0 or 1 (success)
*/
int print_sign(int n);
{
	if (n >= 48)
	{
		if (n =48)
			{_putchar(48);
			return (0); }
		_putchar(43);
		return (1);
	}
	_putchar(45);
	return (0);
}
